{# Tableau de bord de l'administration : affiche les statistiques globales et activités récentes #}
{% extends 'base.html.twig' %}
{% block title %}
	Dashboard Administrateur - Auxilia E-commerce
{% endblock %}

{% block body %}
<div class="container" style="margin-top: 2rem;">
	<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem;">
		<h1 style="font-size: 2.5rem; color: var(--color-primary);">Dashboard Administrateur</h1>
		<a href="{{ path('app_home') }}" class="btn btn-secondary">Retour au site</a>
	</div>

	<!-- Navigation rapide -->
	<div style="background-color: var(--color-light); padding: 2rem; border-radius: 12px; margin-bottom: 3rem;">
		<h2 style="font-size: 1.5rem; color: var(--color-primary); margin-bottom: 1.5rem;">Navigation rapide</h2>
		<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1rem;">
			<a href="{{ path('app_admin_products') }}" class="btn btn-primary" style="text-align: center; padding: 1rem;">
				📦 Gérer les produits
			</a>
			<a href="{{ path('app_admin_users') }}" class="btn btn-secondary" style="text-align: center; padding: 1rem;">
				👥 Gérer les utilisateurs
			</a>
			<a href="{{ path('app_admin_orders') }}" class="btn" style="background-color: #f59e0b; color: white; text-align: center; padding: 1rem;">
				🛒 Gérer les commandes
			</a>
			<a href="{{ path('app_admin_testimonials') }}" class="btn" style="background-color: #8b5cf6; color: white; text-align: center; padding: 1rem;">
				⭐ Les témoignages
			</a>
			<a href="{{ path('app_admin_products', {'stock': 'low'}) }}" class="btn" style="background-color: #ef4444; color: white; text-align: center; padding: 1rem;">
				⚠️ Gestion du stock
			</a>
			<a href="{{ path('app_admin_newsletter') }}" class="btn" style="background-color: #ec4899; color: white; text-align: center; padding: 1rem;">
				📧 Newsletter
			</a>
			<a href="{{ path('app_product') }}" class="btn" style="background-color: #10b981; color: white; text-align: center; padding: 1rem;">
				🌐 Voir le site
			</a>
		</div>
	</div>

	<!-- Statistiques de Ventes -->
	<div style="background: linear-gradient(135deg, #1e3a5f 0%, #2d5a87 100%); padding: 2rem; border-radius: 16px; margin-bottom: 3rem; color: white;">
		<h2 style="font-size: 1.5rem; margin-bottom: 1.5rem; display: flex; align-items: center; gap: 0.75rem;">
			<i class="fas fa-chart-line"></i> Statistiques de Ventes
		</h2>
		<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1.5rem;">
			<div style="background: rgba(255,255,255,0.1); padding: 1.5rem; border-radius: 12px; backdrop-filter: blur(10px);">
				<div style="display: flex; align-items: center; gap: 0.75rem; margin-bottom: 0.5rem;">
					<i class="fas fa-euro-sign" style="font-size: 1.5rem; opacity: 0.8;"></i>
					<span style="opacity: 0.8; font-size: 0.9rem;">CA Total</span>
				</div>
				<p style="font-size: 2rem; font-weight: bold; margin: 0;">{{ sales_stats.total_revenue|number_format(2, ',', ' ') }} €</p>
			</div>
			<div style="background: rgba(255,255,255,0.1); padding: 1.5rem; border-radius: 12px; backdrop-filter: blur(10px);">
				<div style="display: flex; align-items: center; gap: 0.75rem; margin-bottom: 0.5rem;">
					<i class="fas fa-calendar-alt" style="font-size: 1.5rem; opacity: 0.8;"></i>
					<span style="opacity: 0.8; font-size: 0.9rem;">CA ce mois</span>
				</div>
				<p style="font-size: 2rem; font-weight: bold; margin: 0;">{{ sales_stats.monthly_revenue|number_format(2, ',', ' ') }} €</p>
			</div>
			<div style="background: rgba(255,255,255,0.1); padding: 1.5rem; border-radius: 12px; backdrop-filter: blur(10px);">
				<div style="display: flex; align-items: center; gap: 0.75rem; margin-bottom: 0.5rem;">
					<i class="fas fa-clock" style="font-size: 1.5rem; opacity: 0.8;"></i>
					<span style="opacity: 0.8; font-size: 0.9rem;">CA aujourd'hui</span>
				</div>
				<p style="font-size: 2rem; font-weight: bold; margin: 0;">{{ sales_stats.today_revenue|number_format(2, ',', ' ') }} €</p>
			</div>
			<div style="background: rgba(255,255,255,0.1); padding: 1.5rem; border-radius: 12px; backdrop-filter: blur(10px);">
				<div style="display: flex; align-items: center; gap: 0.75rem; margin-bottom: 0.5rem;">
					<i class="fas fa-shopping-basket" style="font-size: 1.5rem; opacity: 0.8;"></i>
					<span style="opacity: 0.8; font-size: 0.9rem;">Panier moyen</span>
				</div>
				<p style="font-size: 2rem; font-weight: bold; margin: 0;">{{ sales_stats.average_order|number_format(2, ',', ' ') }} €</p>
			</div>
		</div>
	</div>

	<!-- Graphiques de ventes -->
	<div style="display: grid; grid-template-columns: 2fr 1fr; gap: 2rem; margin-bottom: 3rem;">
		<!-- Graphique des 7 derniers jours -->
		<div style="background-color: white; border: 1px solid #e0e0e0; border-radius: 12px; padding: 2rem;">
			<h3 style="font-size: 1.25rem; color: var(--color-primary); margin: 0 0 1.5rem 0;">
				<i class="fas fa-chart-bar me-2"></i>Ventes des 7 derniers jours
			</h3>
			<div style="display: flex; align-items: flex-end; justify-content: space-between; height: 200px; gap: 0.5rem; padding-bottom: 1.5rem; border-bottom: 1px solid #e0e0e0;">
				{% set max_revenue = 1 %}
				{% for day in sales_last_7_days %}
					{% if day.revenue > max_revenue %}{% set max_revenue = day.revenue %}{% endif %}
				{% endfor %}
				{% for day in sales_last_7_days %}
					{% set height = max_revenue > 0 ? (day.revenue / max_revenue * 150) : 0 %}
					<div style="flex: 1; display: flex; flex-direction: column; align-items: center; gap: 0.5rem;">
						<span style="font-size: 0.7rem; color: var(--color-text-light);">{{ day.revenue|number_format(0, ',', ' ') }}€</span>
						<div style="width: 100%; max-width: 40px; height: {{ height > 5 ? height : 5 }}px; background: linear-gradient(180deg, var(--color-primary) 0%, var(--color-accent) 100%); border-radius: 4px 4px 0 0; transition: all 0.3s;"></div>
					</div>
				{% endfor %}
			</div>
			<div style="display: flex; justify-content: space-between; padding-top: 0.75rem;">
				{% for day in sales_last_7_days %}
					<div style="flex: 1; text-align: center;">
						<span style="font-size: 0.75rem; color: var(--color-text-light);">{{ day.day }}</span><br>
						<span style="font-size: 0.65rem; color: var(--color-text-light);">{{ day.date }}</span>
					</div>
				{% endfor %}
			</div>
		</div>

		<!-- Répartition par statut -->
		<div style="background-color: white; border: 1px solid #e0e0e0; border-radius: 12px; padding: 2rem;">
			<h3 style="font-size: 1.25rem; color: var(--color-primary); margin: 0 0 1.5rem 0;">
				<i class="fas fa-pie-chart me-2"></i>Commandes par statut
			</h3>
			<div style="display: flex; flex-direction: column; gap: 0.75rem;">
				{% set status_colors = {
					'pending': '#f59e0b',
					'paid': '#10b981',
					'confirmed': '#10b981',
					'shipped': '#8b5cf6',
					'delivered': '#3b82f6',
					'cancelled': '#ef4444'
				} %}
				{% set status_labels = {
					'pending': 'En attente',
					'paid': 'Payée',
					'confirmed': 'Confirmée',
					'shipped': 'Expédiée',
					'delivered': 'Livrée',
					'cancelled': 'Annulée'
				} %}
				{% for status, count in sales_stats.orders_by_status %}
					<div style="display: flex; align-items: center; gap: 1rem;">
						<div style="width: 12px; height: 12px; border-radius: 50%; background-color: {{ status_colors[status]|default('#6b7280') }};"></div>
						<span style="flex: 1; font-size: 0.9rem;">{{ status_labels[status]|default(status) }}</span>
						<span style="font-weight: bold; background-color: {{ status_colors[status]|default('#6b7280') }}20; color: {{ status_colors[status]|default('#6b7280') }}; padding: 0.25rem 0.75rem; border-radius: 9999px; font-size: 0.85rem;">{{ count }}</span>
					</div>
				{% else %}
					<p style="color: var(--color-text-light); text-align: center;">Aucune commande</p>
				{% endfor %}
			</div>
		</div>
	</div>

	<!-- Ventes mensuelles et Top produits -->
	<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; margin-bottom: 3rem;">
		<!-- Graphique des 6 derniers mois -->
		<div style="background-color: white; border: 1px solid #e0e0e0; border-radius: 12px; padding: 2rem;">
			<h3 style="font-size: 1.25rem; color: var(--color-primary); margin: 0 0 1.5rem 0;">
				<i class="fas fa-chart-area me-2"></i>Évolution sur 6 mois
			</h3>
			<div style="display: flex; align-items: flex-end; justify-content: space-between; height: 180px; gap: 1rem; padding-bottom: 1.5rem; border-bottom: 1px solid #e0e0e0;">
				{% set max_monthly = 1 %}
				{% for month in sales_last_6_months %}
					{% if month.revenue > max_monthly %}{% set max_monthly = month.revenue %}{% endif %}
				{% endfor %}
				{% for month in sales_last_6_months %}
					{% set height = max_monthly > 0 ? (month.revenue / max_monthly * 130) : 0 %}
					<div style="flex: 1; display: flex; flex-direction: column; align-items: center; gap: 0.5rem;">
						<span style="font-size: 0.7rem; color: var(--color-text-light);">{{ month.revenue|number_format(0, ',', ' ') }}€</span>
						<div style="width: 100%; height: {{ height > 5 ? height : 5 }}px; background: linear-gradient(180deg, #10b981 0%, #059669 100%); border-radius: 4px 4px 0 0;"></div>
					</div>
				{% endfor %}
			</div>
			<div style="display: flex; justify-content: space-between; padding-top: 0.75rem;">
				{% for month in sales_last_6_months %}
					<div style="flex: 1; text-align: center;">
						<span style="font-size: 0.8rem; color: var(--color-text-light);">{{ month.month }}</span>
					</div>
				{% endfor %}
			</div>
		</div>

		<!-- Top produits vendus -->
		<div style="background-color: white; border: 1px solid #e0e0e0; border-radius: 12px; padding: 2rem;">
			<h3 style="font-size: 1.25rem; color: var(--color-primary); margin: 0 0 1.5rem 0;">
				<i class="fas fa-trophy me-2" style="color: #f59e0b;"></i>Top 5 produits vendus
			</h3>
			{% if top_products|length > 0 %}
				<div style="display: flex; flex-direction: column; gap: 1rem;">
					{% for idx, product in top_products %}
						<div style="display: flex; align-items: center; gap: 1rem; padding: 0.75rem; background-color: var(--color-light); border-radius: 8px;">
							<div style="width: 32px; height: 32px; background: {{ idx == 0 ? 'linear-gradient(135deg, #f59e0b, #d97706)' : (idx == 1 ? 'linear-gradient(135deg, #9ca3af, #6b7280)' : (idx == 2 ? 'linear-gradient(135deg, #d97706, #92400e)' : 'linear-gradient(135deg, var(--color-primary), var(--color-accent))')) }}; border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; font-weight: bold; font-size: 0.85rem;">
								{{ idx + 1 }}
							</div>
							<div style="flex: 1; min-width: 0;">
								<p style="margin: 0; font-weight: 500; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">{{ product.name }}</p>
								<p style="margin: 0; font-size: 0.8rem; color: var(--color-text-light);">{{ product.totalSold }} vendus</p>
							</div>
							<span style="font-weight: bold; color: var(--color-primary);">{{ product.totalRevenue|number_format(0, ',', ' ') }} €</span>
						</div>
					{% endfor %}
				</div>
			{% else %}
				<p style="color: var(--color-text-light); text-align: center; padding: 2rem;">Aucune vente enregistrée</p>
			{% endif %}
		</div>
	</div>

	<!-- Statistiques -->
	<div class="dashboard-stats-grid" style="margin-bottom: 3rem;">
		<a href="{{ path('app_admin_products') }}" class="dashboard-card" style="text-decoration: none;">
			<div style="background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-accent) 100%); padding: 2rem; border-radius: 12px; color: white;">
				<div style="font-size: 3rem; margin-bottom: 0.5rem;">📦</div>
				<h3 style="font-size: 1.25rem; margin-bottom: 0.5rem; opacity: 0.9;">Produits</h3>
				<p style="font-size: 2.5rem; font-weight: bold; margin: 0;">{{ stats.total_products }}</p>
				<span style="color: white; text-decoration: underline; font-size: 0.9rem; margin-top: 0.5rem; display: inline-block;">Voir tous →</span>
			</div>
		</a>

		<a href="{{ path('app_admin_users') }}" class="dashboard-card" style="text-decoration: none;">
			<div style="background: linear-gradient(135deg, #10b981 0%, #059669 100%); padding: 2rem; border-radius: 12px; color: white;">
				<div style="font-size: 3rem; margin-bottom: 0.5rem;">👥</div>
				<h3 style="font-size: 1.25rem; margin-bottom: 0.5rem; opacity: 0.9;">Utilisateurs</h3>
				<p style="font-size: 2.5rem; font-weight: bold; margin: 0;">{{ stats.total_users }}</p>
				<span style="color: white; text-decoration: underline; font-size: 0.9rem; margin-top: 0.5rem; display: inline-block;">Voir tous →</span>
			</div>
		</a>

		<a href="{{ path('app_admin_orders') }}" class="dashboard-card" style="text-decoration: none;">
			<div style="background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%); padding: 2rem; border-radius: 12px; color: white;">
				<div style="font-size: 3rem; margin-bottom: 0.5rem;">🛒</div>
				<h3 style="font-size: 1.25rem; margin-bottom: 0.5rem; opacity: 0.9;">Commandes</h3>
				<p style="font-size: 2.5rem; font-weight: bold; margin: 0;">{{ stats.total_orders }}</p>
				<span style="color: white; text-decoration: underline; font-size: 0.9rem; margin-top: 0.5rem; display: inline-block;">Voir toutes →</span>
			</div>
		</a>

		<a href="{{ path('app_admin_testimonials') }}" class="dashboard-card" style="text-decoration: none;">
			<div style="background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%); padding: 2rem; border-radius: 12px; color: white;">
				<div style="font-size: 3rem; margin-bottom: 0.5rem;">⭐</div>
				<h3 style="font-size: 1.25rem; margin-bottom: 0.5rem; opacity: 0.9;">Témoignages</h3>
				<p style="font-size: 2.5rem; font-weight: bold; margin: 0;">{{ stats.total_testimonials|default(0) }}</p>
				<span style="color: white; text-decoration: underline; font-size: 0.9rem; margin-top: 0.5rem; display: inline-block;">Gérer →</span>
			</div>
		</a>

		<a href="{{ path('app_admin_products', {'stock': 'low'}) }}" class="dashboard-card" style="text-decoration: none;">
			<div style="background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%); padding: 2rem; border-radius: 12px; color: white;">
				<div style="font-size: 3rem; margin-bottom: 0.5rem;">⚠️</div>
				<h3 style="font-size: 1.25rem; margin-bottom: 0.5rem; opacity: 0.9;">Stock faible</h3>
				<p style="font-size: 2.5rem; font-weight: bold; margin: 0;">{{ stats.low_stock_products }}</p>
				<span style="font-size: 0.9rem; opacity: 0.9; display: block;">Produits à réapprovisionner</span>
				<span style="color: white; text-decoration: underline; font-size: 0.9rem; margin-top: 0.5rem; display: inline-block;">Voir tous →</span>
			</div>
		</a>

		<a href="{{ path('app_admin_newsletter') }}" class="dashboard-card" style="text-decoration: none;">
			<div style="background: linear-gradient(135deg, #ec4899 0%, #be185d 100%); padding: 2rem; border-radius: 12px; color: white;">
				<div style="font-size: 3rem; margin-bottom: 0.5rem;">📧</div>
				<h3 style="font-size: 1.25rem; margin-bottom: 0.5rem; opacity: 0.9;">Newsletter</h3>
				<p style="font-size: 2.5rem; font-weight: bold; margin: 0;">{{ stats.total_newsletter|default(0) }}</p>
				<span style="font-size: 0.9rem; opacity: 0.9; display: block;">Abonnés actifs</span>
				<span style="color: white; text-decoration: underline; font-size: 0.9rem; margin-top: 0.5rem; display: inline-block;">Gérer →</span>
			</div>
		</a>
	</div>

	<!-- Contenu en 2 colonnes -->
	<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; margin-bottom: 3rem;">
		<!-- Produits récents -->
		<div style="background-color: white; border: 1px solid #e0e0e0; border-radius: 12px; padding: 2rem;">
			<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem;">
				<h2 style="font-size: 1.5rem; color: var(--color-primary); margin: 0;">Produits récents</h2>
				<a href="{{ path('app_admin_products') }}" style="color: var(--color-primary); text-decoration: none; font-size: 0.9rem;">Voir tout →</a>
			</div>
			{% if recent_products|length > 0 %}
				<div style="display: flex; flex-direction: column; gap: 1rem;">
					{% for product in recent_products %}
						<div style="padding: 1rem; background-color: var(--color-light); border-radius: 8px;">
							<div style="display: flex; justify-content: space-between; align-items: start;">
								<div style="flex: 1;">
									<h3 style="font-size: 1rem; margin: 0 0 0.5rem 0; color: var(--color-text);">{{ product.name }}</h3>
									<p style="font-size: 0.9rem; color: var(--color-text-light); margin: 0 0 0.5rem 0;">
										{{ product.price }} € | Stock: {{ product.stock }}
									</p>
									<span style="font-size: 0.85rem; color: var(--color-text-light);">{{ product.category }}</span>
								</div>
							</div>
						</div>
					{% endfor %}
				</div>
			{% else %}
				<p style="color: var(--color-text-light); text-align: center; padding: 2rem;">Aucun produit</p>
			{% endif %}
		</div>

		<!-- Utilisateurs récents -->
		<div style="background-color: white; border: 1px solid #e0e0e0; border-radius: 12px; padding: 2rem;">
			<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem;">
				<h2 style="font-size: 1.5rem; color: var(--color-primary); margin: 0;">Utilisateurs récents</h2>
				<a href="{{ path('app_admin_users') }}" style="color: var(--color-primary); text-decoration: none; font-size: 0.9rem;">Voir tout →</a>
			</div>
			{% if recent_users|length > 0 %}
				<div style="display: flex; flex-direction: column; gap: 1rem;">
					{% for user in recent_users %}
						<div style="padding: 1rem; background-color: var(--color-light); border-radius: 8px;">
							<div style="display: flex; justify-content: space-between; align-items: center;">
								<div>
									<h3 style="font-size: 1rem; margin: 0 0 0.5rem 0; color: var(--color-text);">{{ user.email }}</h3>
									<span style="font-size: 0.85rem; color: var(--color-text-light);">
										{% if 'ROLE_ADMIN' in user.roles %}👑 Admin{% else %}👤 Utilisateur{% endif %}
									</span>
								</div>
							</div>
						</div>
					{% endfor %}
				</div>
			{% else %}
				<p style="color: var(--color-text-light); text-align: center; padding: 2rem;">Aucun utilisateur</p>
			{% endif %}
		</div>
	</div>

	<!-- Témoignages récents -->
	{% if recent_testimonials is defined and recent_testimonials|length > 0 %}
	<div style="background-color: white; border: 1px solid #e0e0e0; border-radius: 12px; padding: 2rem; margin-bottom: 3rem;">
		<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem;">
			<h2 style="font-size: 1.5rem; color: var(--color-primary); margin: 0;">Témoignages récents</h2>
			<a href="{{ path('app_admin_testimonials') }}" style="color: var(--color-primary); text-decoration: none; font-size: 0.9rem;">Gérer tout →</a>
		</div>
		<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1rem;">
			{% for t in recent_testimonials %}
				<div style="padding: 1.25rem; background-color: var(--color-light); border-radius: 12px; border-left: 4px solid {{ t.isApproved ? '#10b981' : '#f59e0b' }};">
					<div style="display: flex; justify-content: space-between; align-items: flex-start;">
						<div style="flex: 1;">
							<div style="display: flex; align-items: center; gap: 0.75rem; margin-bottom: 0.5rem;">
								<h3 style="font-size: 1rem; margin: 0; color: var(--color-text);">{{ t.name }}</h3>
								<span style="font-size: 0.75rem; padding: 0.2rem 0.6rem; border-radius: 9999px; {{ t.isApproved ? 'background-color: #dcfce7; color: #166534;' : 'background-color: #fef9c3; color: #854d0e;' }}">
									{{ t.isApproved ? 'Approuvé' : 'En attente' }}
								</span>
							</div>
							<div style="color: #f59e0b; margin-bottom: 0.5rem; font-size: 0.9rem;">
								{% for i in 1..5 %}
									{% if i <= t.rating %}<i class="fas fa-star"></i>{% else %}<i class="far fa-star"></i>{% endif %}
								{% endfor %}
							</div>
							<p style="font-size: 0.9rem; color: var(--color-text-light); margin: 0; line-height: 1.4;">{{ t.content|length > 100 ? t.content|slice(0, 100) ~ '...' : t.content }}</p>
						</div>
					</div>
				</div>
			{% endfor %}
		</div>
	</div>
	{% endif %}

	<!-- Derniers abonnés Newsletter -->
	{% if recent_newsletter is defined and recent_newsletter|length > 0 %}
	<div style="background-color: white; border: 1px solid #e0e0e0; border-radius: 12px; padding: 2rem; margin-bottom: 3rem;">
		<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem;">
			<h2 style="font-size: 1.5rem; color: var(--color-primary); margin: 0;">
				<i class="fas fa-envelope me-2" style="color: #ec4899;"></i>Derniers abonnés Newsletter
			</h2>
			<a href="{{ path('app_admin_newsletter') }}" style="color: var(--color-primary); text-decoration: none; font-size: 0.9rem;">Gérer tout →</a>
		</div>
		<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 1rem;">
			{% for subscriber in recent_newsletter %}
				<div style="padding: 1rem; background-color: var(--color-light); border-radius: 8px; display: flex; align-items: center; gap: 1rem;">
					<div style="width: 45px; height: 45px; background: linear-gradient(135deg, #ec4899, #be185d); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; font-weight: bold; font-size: 1.2rem;">
						{{ subscriber.email|slice(0,1)|upper }}
					</div>
					<div style="flex: 1; min-width: 0;">
						<p style="margin: 0; font-weight: 500; color: var(--color-text); white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">{{ subscriber.email }}</p>
						<p style="margin: 0; font-size: 0.8rem; color: var(--color-text-light);">{{ subscriber.subscribedAt|date('d/m/Y à H:i') }}</p>
					</div>
					<span style="padding: 0.2rem 0.5rem; border-radius: 9999px; font-size: 0.7rem; background-color: #dcfce7; color: #166534;">Actif</span>
				</div>
			{% endfor %}
		</div>
	</div>
	{% endif %}

	<!-- Commandes récentes -->
	<div style="background-color: white; border: 1px solid #e0e0e0; border-radius: 12px; padding: 2rem;">
		<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem;">
			<h2 style="font-size: 1.5rem; color: var(--color-primary); margin: 0;">Commandes récentes</h2>
			<a href="{{ path('app_admin_orders') }}" style="color: var(--color-primary); text-decoration: none; font-size: 0.9rem;">Voir toutes →</a>
		</div>
		{% if recent_orders|length > 0 %}
			<div style="overflow-x: auto;">
				<table style="width: 100%; border-collapse: collapse;">
					<thead>
						<tr style="border-bottom: 2px solid #e0e0e0;">
							<th style="padding: 1rem; text-align: left; color: var(--color-text);">ID</th>
							<th style="padding: 1rem; text-align: left; color: var(--color-text);">Date</th>
							<th style="padding: 1rem; text-align: left; color: var(--color-text);">Total</th>
							<th style="padding: 1rem; text-align: left; color: var(--color-text);">Statut</th>
						</tr>
					</thead>
					<tbody>
						{% for order in recent_orders %}
							<tr style="border-bottom: 1px solid #e0e0e0;">
								<td style="padding: 1rem;">#{{ order.id }}</td>
								<td style="padding: 1rem;">{{ order.dateat|date('d/m/Y H:i') }}</td>
								<td style="padding: 1rem; font-weight: bold; color: var(--color-primary);">{{ order.total }} €</td>
								<td style="padding: 1rem;">
									{% set status_info = {
										'paid': {'label': 'Payée', 'style': 'background-color: #dcfce7; color: #166534;'},
										'confirmed': {'label': 'Confirmée', 'style': 'background-color: #dcfce7; color: #166534;'},
										'pending': {'label': 'En attente', 'style': 'background-color: #fef9c3; color: #854d0e;'},
										'delivered': {'label': 'Livrée', 'style': 'background-color: #dbeafe; color: #1e40af;'},
										'cancelled': {'label': 'Annulée', 'style': 'background-color: #fee2e2; color: #991b1b;'},
										'shipped': {'label': 'Expédiée', 'style': 'background-color: #f3e8ff; color: #6b21a8;'}
									} %}
									{% set info = status_info[order.status|lower] ?? {'label': order.status, 'style': 'background-color: #f1f5f9; color: #475569;'} %}
									<span style="padding: 0.4rem 0.8rem; border-radius: 9999px; font-size: 0.75rem; font-weight: 600; {{ info.style }} display: inline-block; white-space: nowrap;">
										<i class="fas fa-circle" style="font-size: 0.5rem; margin-right: 0.4rem; vertical-align: middle; opacity: 0.7;"></i>
										{{ info.label }}
									</span>
								</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</div>
		{% else %}
			<p style="color: var(--color-text-light); text-align: center; padding: 2rem;">Aucune commande</p>
		{% endif %}
	</div>
</div>
{% endblock %}
